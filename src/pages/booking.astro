---
import { DEFAULT_LANG } from '../lib/i18n';

const search = Astro.url.search;
const destination = search ? `/${DEFAULT_LANG}/booking${search}` : `/${DEFAULT_LANG}/booking`;
return Astro.redirect(destination, 307);
---
